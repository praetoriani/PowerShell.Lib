<?xml version="1.0" encoding="utf-8"?>
<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="DAMSA - Download-Links auswählen"
    Width="800"
    Height="600"
    WindowStartupLocation="CenterScreen"
    ResizeMode="CanMinimize"
    Background="#FF1E1E1E">
    
    <Window.Resources>
        <!-- Font Resources -->
        <FontFamily x:Key="ExoRegular">Exo 2</FontFamily>
        <FontFamily x:Key="ExoBold">Exo 2 SemiBold</FontFamily>
        
        <!-- Button Style with Hover and Click Effects -->
        <Style x:Key="DAMSAButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="{StaticResource ExoRegular}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF007ACC"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF1BA1E2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF005A9E"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF0E5A8A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- ListBox Style -->
        <Style x:Key="DAMSAListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="{StaticResource ExoRegular}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
        
        <!-- ListBoxItem Style -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF007ACC"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#FF1BA1E2"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   x:Name="TitleText"
                   Text="DAMSA - Download-Links auswählen"
                   FontFamily="{StaticResource ExoBold}"
                   FontSize="24"
                   Foreground="White"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>
        
        <!-- Info Text -->
        <TextBlock Grid.Row="1" 
                   x:Name="InfoText"
                   Text="Die folgenden Download-URLs wurden gefunden. Bitte wählen Sie alle Pakete aus, die heruntergeladen werden sollen:"
                   FontFamily="{StaticResource ExoRegular}"
                   FontSize="14"
                   Foreground="#FFCCCCCC"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>
        
        <!-- Download Links List -->
        <ListBox Grid.Row="2" 
                 x:Name="DownloadLinksListBox"
                 Style="{StaticResource DAMSAListBoxStyle}"
                 SelectionMode="Multiple"
                 Margin="0,0,0,20">
            <!-- Items will be populated dynamically -->
        </ListBox>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center">
            <Button x:Name="DownloadButton"
                    Content="Download"
                    Style="{StaticResource DAMSAButtonStyle}"
                    Width="120"
                    Height="35"/>
            
            <Button x:Name="ExitButton"
                    Content="Beenden"
                    Style="{StaticResource DAMSAButtonStyle}"
                    Width="120"
                    Height="35"/>
        </StackPanel>
    </Grid>
</Window>