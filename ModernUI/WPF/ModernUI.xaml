<Window 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="ModernUI"
    Height="600"
    Width="800"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    x:Name="MainWindow">

    <Grid>
        <!-- BACKGROUND IMAGE LAYER -->
        <Image 
            x:Name="BackgroundImage" 
            Stretch="UniformToFill" 
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch" />
        
        <!-- MAIN OVERLAY GRID -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- TITLE BAR -->
            <Border x:Name="TitleBar" Grid.Row="0" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Opacity="0.95">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Window Icon (Left) -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                        <Image 
                            x:Name="WindowIcon" 
                            Width="24" 
                            Height="24" 
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center" />
                    </StackPanel>

                    <!-- Window Title (Center-Left) -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                        <TextBlock 
                            x:Name="TitleText"
                            Text="ModernUI"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"
                            FontSize="14"
                            Foreground="#333333"
                            FontWeight="SemiBold" />
                        <TextBlock
                            Text=" " />
                        <TextBlock 
                            x:Name="VersionText"
                            Text="v1.00.02"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"
                            FontSize="12"
                            Foreground="#666666" />
                    </StackPanel>

                    <!-- Window Controls (Right) -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                        <!-- Close Button as Label with Image -->
                        <Label 
                            x:Name="CloseButton" 
                            Width="24" 
                            Height="24" 
                            Cursor="Hand"
                            HorizontalContentAlignment="Center" 
                            VerticalContentAlignment="Center">
                            <Image 
                                x:Name="CloseButtonImage" 
                                Width="24" 
                                Height="24" 
                                Stretch="Uniform" />
                        </Label>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- MAIN CONTENT AREA -->
            <Grid Grid.Row="1" Background="Transparent">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="40">
                    <!-- Main Title -->
                    <TextBlock 
                        Text="ModernUI" 
                        FontSize="32" 
                        FontWeight="Bold" 
                        Foreground="#FFFFFF"
                        TextAlignment="Center"
                        Margin="0,0,0,8" />

                    <!-- Version Text (slightly smaller) -->
                    <TextBlock 
                        x:Name="MainVersionText"
                        Text="v1.00.02" 
                        FontSize="20" 
                        FontWeight="SemiBold" 
                        Foreground="#E0E0E0"
                        TextAlignment="Center"
                        Margin="0,0,0,24" />

                    <!-- App Screen Image Layer -->
                    <Border Background="#1E1E1E" CornerRadius="8" Padding="8" Margin="0,0,0,24" Opacity="0.95">
                        <Image 
                            x:Name="AppScreenImage"
                            Stretch="Uniform"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Height="260" />
                    </Border>

                    <!-- Credits -->
                    <TextBlock 
                        Text="Written by Praetoriani" 
                        FontSize="14" 
                        Foreground="#FFFFFF"
                        TextAlignment="Center"
                        Margin="0,0,0,4" />

                    <TextBlock 
                        Text="Now available on GitHub" 
                        FontSize="13" 
                        Foreground="#B0BEC5"
                        TextAlignment="Center"
                        Margin="0,0,0,0" />
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
