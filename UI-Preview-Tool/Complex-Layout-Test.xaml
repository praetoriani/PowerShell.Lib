<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Complex Layout Test" Height="500" Width="700">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel -->
        <Border Grid.Column="0" Background="#FFE3F2FD" BorderBrush="#FF1976D2" BorderThickness="1">
            <StackPanel Margin="10">
                <TextBlock Text="Navigation" FontWeight="Bold" Margin="0,0,0,10"/>
                <ListBox Height="200">
                    <ListBoxItem Content="Dashboard"/>
                    <ListBoxItem Content="Settings"/>
                    <ListBoxItem Content="Reports"/>
                    <ListBoxItem Content="Help"/>
                </ListBox>

                <TextBlock Text="Tools" FontWeight="Bold" Margin="0,20,0,10"/>
                <Button Content="Export" Margin="0,5"/>
                <Button Content="Import" Margin="0,5"/>
                <Button Content="Reset" Margin="0,5"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Toolbar -->
            <ToolBarTray Grid.Row="0">
                <ToolBar>
                    <Button Content="New"/>
                    <Button Content="Open"/>
                    <Button Content="Save"/>
                    <Separator/>
                    <Button Content="Copy"/>
                    <Button Content="Paste"/>
                </ToolBar>
            </ToolBarTray>

            <!-- Content Area -->
            <TabControl Grid.Row="1" Margin="0,10">
                <TabItem Header="Tab 1">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Filter: " VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Width="200" Margin="0,0,10,0"/>
                            <Button Content="Search" Padding="10,2"/>
                        </StackPanel>

                        <DataGrid Grid.Row="1" AutoGenerateColumns="True">
                            <!-- Would normally display data from ItemsSource -->
                        </DataGrid>
                    </Grid>
                </TabItem>

                <TabItem Header="Tab 2">
                    <ScrollViewer>
                        <StackPanel Margin="10">
                            <TextBlock Text="Detail View" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

                            <GroupBox Header="General Information" Margin="0,0,0,10">
                                <Grid Margin="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" Margin="0,0,10,5"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,5"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Description:" Margin="0,0,10,5"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,5"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Status:" Margin="0,0,10,5"/>
                                    <ComboBox Grid.Row="2" Grid.Column="1">
                                        <ComboBoxItem Content="Active"/>
                                        <ComboBoxItem Content="Inactive"/>
                                        <ComboBoxItem Content="Maintenance"/>
                                    </ComboBox>
                                </Grid>
                            </GroupBox>

                            <GroupBox Header="Advanced Options">
                                <StackPanel Margin="10">
                                    <CheckBox Content="Enable option A" Margin="0,5"/>
                                    <CheckBox Content="Enable option B" Margin="0,5"/>
                                    <CheckBox Content="Debugging mode" Margin="0,5"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                                        <Button Content="Apply" Padding="15,5" Margin="0,0,10,0"/>
                                        <Button Content="Reset" Padding="15,5"/>
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>

            <!-- Status Bar -->
            <StatusBar Grid.Row="2">
                <StatusBarItem Content="Ready"/>
                <Separator/>
                <StatusBarItem Content="Items: 42"/>
                <StatusBarItem HorizontalAlignment="Right" Content="12:34:56"/>
            </StatusBar>
        </Grid>
    </Grid>
</Window>
