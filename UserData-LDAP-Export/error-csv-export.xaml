<Window 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="CSV-Exportfehler" 
    Width="480"
    Height="360" 
    WindowStartupLocation="CenterScreen"
    ResizeMode="NoResize"
    WindowStyle="SingleBorderWindow"
    ShowInTaskbar="False"
    Topmost="True">

    <Window.Resources>
        <Style x:Key="ExitButton" TargetType="Button">
            <Setter Property="Background" Value="#FFE74C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFC0392B"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFA93226"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FFCCCCCC"/>
                                <Setter Property="Foreground" Value="#FF666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Background="#FFE74C3C">
            <TextBlock 
                Text="💾" 
                FontSize="24" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Left" 
                Margin="20,0,0,0" 
                Foreground="White"/>
            <TextBlock 
                Text="CSV-Exportfehler" 
                FontSize="16" 
                FontWeight="SemiBold" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Left" 
                Margin="60,0,0,0" 
                Foreground="White"/>
        </Grid>

        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="25,15,25,15" VerticalAlignment="Top">
            <TextBlock 
                Text="CSV-Datei konnte nicht erstellt werden!" 
                FontSize="16" 
                FontWeight="Bold"
                TextAlignment="Center"
                Margin="0,0,0,15" 
                Foreground="#FFE74C3C"/>
            <TextBlock 
                Text="Beim Erstellen oder Speichern der CSV-Datei ist ein Fehler aufgetreten." 
                FontSize="12" 
                TextAlignment="Center"
                TextWrapping="Wrap" 
                Margin="0,0,0,10"
                Foreground="#FF333333"/>
            <TextBlock 
                Text="Mögliche Ursachen:" 
                FontSize="12" 
                FontWeight="SemiBold"
                TextAlignment="Left"
                Margin="0,10,0,5"
                Foreground="#FF333333"/>
            <TextBlock 
                Text="• Unzureichende Schreibberechtigungen im Zielverzeichnis&#x0A;• Nicht genügend Speicherplatz auf dem Laufwerk&#x0A;• Die Datei ist bereits geöffnet oder wird von einem anderen Prozess verwendet&#x0A;• Ungültiger Dateipfad oder Dateiname"
                FontSize="11" 
                TextAlignment="Left"
                TextWrapping="Wrap" 
                Margin="10,0,0,0"
                LineHeight="16"
                LineStackingStrategy="BlockLineHeight"
                Foreground="#FF666666"/>
        </StackPanel>

        <!-- Footer -->
        <Grid Grid.Row="2" Background="#FFF5F5F5">
            <Button 
                Name="btnExit" 
                Content="Beenden" 
                Style="{StaticResource ExitButton}"
                Width="100" 
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>