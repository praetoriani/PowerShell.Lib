<Window 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="{DynamicResource WindowTitle}" 
    Width="450" 
    Height="380" 
    WindowStartupLocation="CenterScreen"
    ResizeMode="NoResize"
    WindowStyle="SingleBorderWindow"
    ShowInTaskbar="False"
    Topmost="True">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF106EBE"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button">
            <Setter Property="Background" Value="#FFE1E1E1"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFADADAD"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFD0D0D0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="InputField" TargetType="Control">
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderBrush" Value="#FFADADAD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </Window.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Background="#FF0078D4">
            <TextBlock 
                Text="🔐" 
                FontSize="24" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Left" 
                Margin="20,0,0,0" 
                Foreground="White"/>
            <TextBlock 
                Name="HeaderText"
                Text="{DynamicResource WindowTitle}" 
                FontSize="16" 
                FontWeight="SemiBold" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Left" 
                Margin="60,0,0,0" 
                Foreground="White"/>
        </Grid>

        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="30,20,30,20">
            <TextBlock 
                Name="MessageText"
                Text="{DynamicResource DialogMessage}" 
                FontSize="12" 
                TextWrapping="Wrap" 
                Margin="0,0,0,20" 
                Foreground="#FF333333"/>

            <Label Content="Benutzername:" FontSize="12" FontWeight="SemiBold" Padding="0,0,0,5"/>
            <TextBox 
                Name="txtUsername" 
                Style="{StaticResource InputField}"
                Padding="10,6"/>

            <Label Content="Passwort:" FontSize="12" FontWeight="SemiBold" Padding="0,0,0,5"/>
            <PasswordBox 
                Name="txtPassword" 
                Style="{StaticResource InputField}"
                Padding="10,6"/>
        </StackPanel>

        <!-- Footer -->
        <Grid Grid.Row="2" Background="#FFF5F5F5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,30,0">
                <Button 
                    Name="btnCancel" 
                    Content="Abbrechen" 
                    Style="{StaticResource CancelButton}"
                    Width="90" 
                    Margin="0,0,10,0"/>
                <Button 
                    Name="btnOK" 
                    Content="OK" 
                    Style="{StaticResource ModernButton}"
                    Width="90" 
                    IsDefault="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>